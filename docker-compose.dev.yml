services:
  odori-web:
    build: .
    environment: !reset []
    env_file: .env
    ports: ["8000:8000"]
    networks: !override [net]
    labels: !reset []
  odori-worker:
    build: .
    environment: !reset []
    env_file: .env
    networks: !override [net]
  odori-migrate:
    build: .
    environment: !reset []
    env_file: .env
    networks: !override [net]
  postgres:
    environment: !reset []
    env_file: .env
    ports: ["5432:5432"]
    networks: !override [net]
networks:
  proxy: !reset null
  internal: !reset null
  net: {
    internal: false
  }
volumes:
  postgres:
    name: odori_dev_postgres
  uploads:
    name: odori_dev_uploads
